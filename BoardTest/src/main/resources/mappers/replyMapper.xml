<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.replyMapper">
	<select id="list" resultType="com.raonis.board.domain.ReplyVO">
		select * from tbl_reply where num=#{num} order by rno desc
	</select>
	
	<insert id="write">
		insert into tbl_reply (rno, num, replytext, replyer) values(seq_reply.nextVal, #{num}, #{replytext}, #{replyer})
	</insert>
	
	<update id="addReplyCount">
		update tbl_qna set replycnt=replycnt+1 where num=#{num} 
	</update>
</mapper>